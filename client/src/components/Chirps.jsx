import React, { Component } from 'react';
import { render } from 'react-dom';

class Chirps extends Component {

    constructor(props) {
        super(props);
    }
    
    render() {
        return <h4>You are in the CHIRPS PAGE.</h4>;
    }
}

export default Chirps;